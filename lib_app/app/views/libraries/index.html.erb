<% @libraries.each do |library| %>
    <div class="container">
        <div class="row">
          <h3><%= library.name %></h3>
          <% if @current_user %>
            <%= button_to "Join", library_users_path(library) %>
          <% end %>
        </div>
    </div>
<% end %>
