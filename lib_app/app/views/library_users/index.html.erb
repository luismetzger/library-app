<div class="container">
    <div class="row">
      <h2><%= @user.first_name %> is a member of the following libraries</h2>

      <ul>
          <% @libraries.each do |lib| %>
            <li><%= lib.name %></li>        
          <% end %>
      </ul>
    </div>
</div>
